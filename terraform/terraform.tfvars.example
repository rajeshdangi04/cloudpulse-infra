# Copy this file to terraform.tfvars and fill in your values
# Usage: cp terraform.tfvars.example terraform.tfvars

region   = "ap-south-1"
key_name = "my-key-pair"  # AWS EC2 Key Pair name (create in AWS Console first)
